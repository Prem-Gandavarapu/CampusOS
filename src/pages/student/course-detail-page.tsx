import { ArrowLeft, BookOpen, CheckCircle2, ClipboardCheck, FileText, PlayCircle, Sparkles } from 'lucide-react'
import { Link, Navigate, useParams } from 'react-router-dom'
import { PageContainer } from '../../components/layout/app-shell'
import { PageTransition } from '../../components/shared/page-transition'
import { ProgressBar } from '../../components/shared/progress-bar'
import { Badge } from '../../components/ui/badge'
import { Card, CardDescription, CardHeader, CardTitle } from '../../components/ui/card'
import { courses } from '../../data/mock-data'

export function CourseDetailPage() { const { courseId } = useParams(); const course = courses.find((item) => item.id === courseId); if (!course) return <Navigate replace to="/student/courses" />; return <PageTransition><PageContainer className="space-y-7"><Link className="inline-flex items-center gap-2 text-sm font-medium text-muted-foreground hover:text-foreground" to="/student/courses"><ArrowLeft className="size-4" />All courses</Link><section className={`relative overflow-hidden rounded-3xl bg-gradient-to-br ${course.color} p-6 text-white shadow-float sm:p-8`}><div className="absolute -right-20 -top-24 size-72 rounded-full bg-white/10 blur-3xl" /><div className="relative"><Badge className="bg-white/15 text-white" variant="outline">{course.code}</Badge><h2 className="mt-4 text-3xl font-semibold tracking-[-0.055em] sm:text-4xl">{course.title}</h2><p className="mt-2 text-sm text-white/75">{course.faculty} · Next class {course.nextClass}</p><div className="mt-7 max-w-md"><div className="mb-2 flex justify-between text-xs text-white/75"><span>Course progress</span><b className="text-white">{course.progress}%</b></div><ProgressBar className="bg-white/20" value={course.progress} /></div></div></section><section className="grid gap-5 xl:grid-cols-[minmax(0,1.25fr)_minmax(18rem,.75fr)]"><Card><CardHeader><div><CardTitle>Syllabus & learning modules</CardTitle><CardDescription>A focused pathway through the rest of the term.</CardDescription></div></CardHeader><ol className="space-y-2">{course.syllabus.map((item, index) => <li className="flex items-center gap-3 rounded-xl p-3 hover:bg-muted/55" key={item}><span className="grid size-7 place-items-center rounded-lg bg-muted text-xs font-semibold text-muted-foreground">{index + 1}</span><span className="flex-1 text-sm font-medium text-foreground">{item}</span>{index < 3 ? <CheckCircle2 className="size-4 text-success" /> : <span className="size-2 rounded-full bg-border-strong" />}</li>)}</ol></Card><Card><CardHeader><div><CardTitle>Course health</CardTitle><CardDescription>Stay on top without overthinking it.</CardDescription></div></CardHeader><div className="space-y-5"><CourseStat label="Attendance" value={`${course.attendance}%`} /><CourseStat label="Assignments" value={`${course.assignments.done} / ${course.assignments.total}`} /><CourseStat label="Modules complete" value={`${Math.round(course.progress / 25)} / 4`} /></div></Card></section><section className="grid gap-5 xl:grid-cols-[minmax(0,1.25fr)_minmax(18rem,.75fr)]"><Card><CardHeader><div><CardTitle>Materials</CardTitle><CardDescription>Pick up exactly where you left off.</CardDescription></div></CardHeader><div className="space-y-2">{course.materials.map((material) => <button className="flex w-full items-center gap-3 rounded-xl p-3 text-left transition-colors hover:bg-muted" key={material.title} type="button"><span className="grid size-9 place-items-center rounded-xl bg-brand/10 text-brand">{material.kind === 'Video' ? <PlayCircle className="size-4" /> : material.kind === 'Slides' ? <FileText className="size-4" /> : <BookOpen className="size-4" />}</span><span className="flex-1"><span className="block text-sm font-medium text-foreground">{material.title}</span><span className="mt-0.5 block text-xs text-muted-foreground">{material.kind} · {material.duration}</span></span></button>)}</div></Card><Card className="bg-brand/[0.055]"><div className="flex gap-3"><span className="grid size-9 shrink-0 place-items-center rounded-xl bg-brand/10 text-brand"><Sparkles className="size-[18px]" /></span><div><h3 className="text-sm font-semibold text-foreground">AI learning insight</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">You are progressing well. Before the next class, revisit the neural network walkthrough and spend 15 minutes comparing model evaluation metrics.</p><Link className="mt-3 inline-flex items-center gap-1 text-xs font-semibold text-brand" to="/student/ai">Ask about this course <ArrowLeft className="size-3.5 rotate-180" /></Link></div></div></Card></section><Card><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-warning/10 text-warning"><ClipboardCheck className="size-5" /></span><div><p className="text-sm font-semibold text-foreground">Next assignment · Classifier performance analysis</p><p className="mt-1 text-sm text-muted-foreground">Due tomorrow. You have completed the data and training sections; evaluation is the final stretch.</p></div></div></Card></PageContainer></PageTransition> }
function CourseStat({ label, value }: { label: string; value: string }) { return <div className="flex items-center justify-between border-b border-border pb-4 last:border-0 last:pb-0"><span className="text-sm text-muted-foreground">{label}</span><span className="text-sm font-semibold text-foreground">{value}</span></div> }
