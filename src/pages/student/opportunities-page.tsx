import { ArrowRight, BriefcaseBusiness, Filter, Sparkles, Target } from 'lucide-react'
import { motion } from 'framer-motion'
import { Link } from 'react-router-dom'
import { OpportunityCard } from '../../components/opportunities/opportunity-card'
import { PageContainer } from '../../components/layout/app-shell'
import { PageTransition } from '../../components/shared/page-transition'
import { Badge } from '../../components/ui/badge'
import { Card } from '../../components/ui/card'
import { opportunities, student } from '../../data/mock-data'

export function OpportunitiesPage() {
  return <PageTransition><PageContainer className="space-y-7"><header className="flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><div className="flex items-center gap-2"><Badge variant="ai"><Sparkles className="size-3" />Opportunity intelligence</Badge><span className="text-xs text-muted-foreground">Matched to {student.program}</span></div><h1 className="mt-3 text-3xl font-semibold tracking-[-.06em] text-foreground sm:text-4xl">Find your next opening.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground sm:text-base">A focused view of hackathons, internships, placements, and workshops that fit your current trajectory.</p></div><Link className="inline-flex h-10 items-center gap-2 rounded-xl bg-brand px-4 text-sm font-semibold text-brand-foreground shadow-sm transition hover:-translate-y-0.5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" to="/student/career"><Target className="size-4" />Open career plan</Link></header><section className="grid gap-4 sm:grid-cols-3"><Card className="p-4"><p className="text-xs text-muted-foreground">Best current match</p><p className="mt-2 text-2xl font-semibold tracking-[-.05em] text-foreground">{opportunities[0].match}%</p><p className="mt-1 text-xs font-medium text-success">Build for India 2026</p></Card><Card className="p-4"><p className="text-xs text-muted-foreground">Open opportunities</p><p className="mt-2 text-2xl font-semibold tracking-[-.05em] text-foreground">{opportunities.length}</p><p className="mt-1 text-xs font-medium text-muted-foreground">Across four paths</p></Card><Card className="p-4"><p className="text-xs text-muted-foreground">AI recommendation</p><p className="mt-2 text-sm font-semibold text-foreground">Lead with your ML project.</p><p className="mt-1 text-xs text-muted-foreground">It strengthens every top match.</p></Card></section><div className="flex items-center justify-between gap-3"><div><h2 className="text-lg font-semibold tracking-[-.03em] text-foreground">Curated for you</h2><p className="mt-1 text-sm text-muted-foreground">Sorted by fit, deadline, and momentum.</p></div><button className="inline-flex h-10 items-center gap-2 rounded-xl border border-border bg-surface px-3 text-sm font-semibold text-muted-foreground transition hover:border-border-strong hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring" type="button"><Filter className="size-4" />Filters</button></div><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{opportunities.map((opportunity, index) => <motion.div animate={{ opacity: 1, y: 0 }} initial={{ opacity: 0, y: 12 }} transition={{ delay: index * .06 }} key={opportunity.id}><OpportunityCard opportunity={opportunity} /></motion.div>)}</section><Card className="flex flex-col gap-4 border-brand/15 bg-brand/[.045] p-5 sm:flex-row sm:items-center sm:justify-between sm:p-6"><div className="flex items-start gap-3"><span className="grid size-10 place-items-center rounded-xl bg-brand/10 text-brand"><BriefcaseBusiness className="size-5" /></span><div><p className="text-sm font-semibold text-foreground">Need a more specific search?</p><p className="mt-1 text-sm text-muted-foreground">Ask Campus AI to find roles by skill, deadline, or career target.</p></div></div><Link className="inline-flex items-center gap-2 text-sm font-semibold text-brand hover:text-brand-secondary" to="/student/ai">Ask Campus AI <ArrowRight className="size-4" /></Link></Card></PageContainer></PageTransition>
}
