import { AnimatePresence, motion } from 'framer-motion'
import { Command, X } from 'lucide-react'
import { CopilotConversation } from './copilot-conversation'

export function GlobalCommand({ open, onClose }: { open: boolean; onClose: () => void }) {
  return <AnimatePresence>{open ? <motion.div animate={{ opacity: 1 }} className="fixed inset-0 z-[90] flex items-end bg-foreground/25 p-3 backdrop-blur-sm sm:items-center sm:justify-center sm:p-6" exit={{ opacity: 0 }} initial={{ opacity: 0 }} onMouseDown={onClose} role="presentation"><motion.section animate={{ opacity: 1, y: 0, scale: 1 }} aria-label="Ask CampusOS" aria-modal="true" className="w-full max-w-2xl rounded-3xl border border-border bg-surface p-4 shadow-float sm:p-5" exit={{ opacity: 0, y: 12, scale: 0.98 }} initial={{ opacity: 0, y: 16, scale: 0.98 }} onMouseDown={(event) => event.stopPropagation()} role="dialog"><header className="mb-4 flex items-center justify-between gap-3"><div className="flex items-center gap-2.5"><span className="grid size-9 place-items-center rounded-xl bg-brand/10 text-brand"><Command className="size-5" /></span><div><p className="text-sm font-semibold tracking-tight text-foreground">Ask CampusOS</p><p className="text-xs text-muted-foreground">Your campus copilot</p></div></div><button aria-label="Close command interface" className="grid size-9 place-items-center rounded-xl text-muted-foreground hover:bg-muted hover:text-foreground" onClick={onClose} type="button"><X className="size-5" /></button></header><CopilotConversation compact /></motion.section></motion.div> : null}</AnimatePresence>
}
